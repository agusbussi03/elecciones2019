{% extends "layout.html.twig" %}

{% block content %}
    {% include "navigator.html.twig" %}
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Carga por circuito</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        Informacion de carga por circuitos: mesas testigos, mesas cargadas y mesas faltantes
                    </div>
                    <div class="panel-body">
                        <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                            <thead>
                                <tr>
                                    <th>Seccion</th>
                                    <th>Circuito</th>
                                    <th>Nombre</th>
                                    <th>Mesas testigos</th>
                                    <th>INTENDENTE cargadas</th>
                                    <th>CONCEJALES faltantes</th>                                    


                                    <th>Reporte</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for item in circuitos%}
                                    <tr class="odd gradeX">
                                        <td>{{item.datos.sec}}</td>
                                        <td>{{item.datos.cirnro}}{{item.datos.cirlet}}</td>
                                        <td>{{item.datos.nomb}}</td>
                                        <td>{{item.datos.cuenta}}</td>
                                        </td>
                                        <td>
                                            <div class="panel panel-default">
                                                <div class="panel-heading">
                                                    <h4 class="panel-title">
                                                        <a data-toggle="collapse" data-parent="#concejal_I_C{{item.datos.sec}}-{{item.datos.cirnro}}{{item.datos.cirlet}}" href="#collapse_I_C{{item.datos.sec}}-{{item.datos.cirnro}}{{item.datos.cirlet}}" aria-expanded="false" class="collapsed">CARGADAS: {{item.intendente.cargadas|length}}</a>
                                                    </h4>
                                                </div>
                                                <div id="collapse_I_C{{item.datos.sec}}-{{item.datos.cirnro}}{{item.datos.cirlet}}" class="panel-collapse collapse" aria-expanded="false" style="height: 0px;">
                                                    <div class="panel-body">
                                                        {% for item2 in item.intendente.cargadas %}
                                                            <button type="button" class="btn btn-primary" style="width:60px"
                                                                    onclick="window.location.href = ''">{{item2.mesa}}</button>
                                                        {%endfor%}        
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="panel panel-default">
                                                <div class="panel-heading">
                                                    <h4 class="panel-title">
                                                        <a data-toggle="collapse" data-parent="#concejal_I_N{{item.datos.sec}}-{{item.datos.cirnro}}{{item.datos.cirlet}}" href="#collapse_I_N{{item.datos.sec}}-{{item.datos.cirnro}}{{item.datos.cirlet}}" aria-expanded="false" class="collapsed">NO CARGADAS: {{item.intendente.nocargadas|length}}</a>
                                                    </h4>
                                                </div>
                                                <div id="collapse_I_N{{item.datos.sec}}-{{item.datos.cirnro}}{{item.datos.cirlet}}" class="panel-collapse collapse" aria-expanded="false" style="height: 0px;">
                                                    <div class="panel-body">
                                                        {% for item2 in item.intendente.nocargadas %}
                                                            <button type="button" class="btn btn-primary" style="width:60px"
                                                                    onclick="window.location.href = ''">{{item2.mesa}}</button>
                                                        {%endfor%}  
                                                    </div>
                                                </div>
                                            </div>

                                        </td>
                                        <td>
                                            <div class="panel panel-default">
                                                <div class="panel-heading">
                                                    <h4 class="panel-title">
                                                        <a data-toggle="collapse" data-parent="#concejal_C_C{{item.datos.sec}}-{{item.datos.cirnro}}{{item.datos.cirlet}}" href="#collapse_C_C{{item.datos.sec}}-{{item.datos.cirnro}}{{item.datos.cirlet}}" aria-expanded="false" class="collapsed">CARGADAS: {{item.concejal.cargadas|length}}</a>
                                                    </h4>
                                                </div>
                                                <div id="collapse_C_C{{item.datos.sec}}-{{item.datos.cirnro}}{{item.datos.cirlet}}" class="panel-collapse collapse" aria-expanded="false" style="height: 0px;">
                                                    <div class="panel-body">
                                                        {% for item2 in item.concejal.cargadas %}
                                                            <button type="button" class="btn btn-primary" style="width:60px"
                                                                    onclick="window.location.href = ''">{{item2.mesa}}</button>
                                                        {%endfor%}        
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="panel panel-default">
                                                <div class="panel-heading">
                                                    <h4 class="panel-title">
                                                        <a data-toggle="collapse" data-parent="#concejal_C_N{{item.datos.sec}}-{{item.datos.cirnro}}{{item.datos.cirlet}}" href="#collapse_C_N{{item.datos.sec}}-{{item.datos.cirnro}}{{item.datos.cirlet}}" aria-expanded="false" class="collapsed">NO CARGADAS: {{item.concejal.nocargadas|length}}</a>
                                                    </h4>
                                                </div>
                                                <div id="collapse_C_N{{item.datos.sec}}-{{item.datos.cirnro}}{{item.datos.cirlet}}" class="panel-collapse collapse" aria-expanded="false" style="height: 0px;">
                                                    <div class="panel-body">
                                                        {% for item2 in item.concejal.nocargadas %}
                                                            <button type="button" class="btn btn-primary" style="width:60px"
                                                                    onclick="window.location.href = ''">{{item2.mesa}}</button>
                                                        {%endfor%}  
                                                    </div>
                                                </div>
                                            </div>                                        
                                        <td><button type="button" class="btn btn-primary" 
                                                    onclick="window.location.href = '{{path('homepage')}}res_concejales/{{item.datos.sec}}/{{item.datos.cirnro}}{%if item.datos.cirlet!=''%}}/{{item.datos.cirlet}}{% endif %}'">Resultados concejal</button>
                                        </td>
                                    </tr>
                                {% endfor %}

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>    
{% endblock %}    
{% block final %}
    <!-- DataTables JavaScript -->
    <script src="{{ asset('/vendor/datatables/js/jquery.dataTables.min.js') }}"></script>
    <script src="{{ asset('/vendor/datatables-plugins/dataTables.bootstrap.min.js') }}"></script>
    <script src="{{ asset('vendor/datatables-responsive/dataTables.responsive.js') }}"></script>
    <script>
                                                        $(document).ready(function () {
                                                            $('#dataTables-example').DataTable({
                                                                responsive: true
                                                            });
                                                        });
    </script>
{% endblock %}


